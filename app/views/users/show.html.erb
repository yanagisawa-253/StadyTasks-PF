<div class="container">
  <div class-"row">
    <div class="col-sm-10 offset-2 mt-4 pb-5 mx-auto border-bottom">
      <table class="table table-borderless">
        <tbody>
          <tr>
            <th>
              <!--エラー箇所-->
              <%= attachment_image_tag @user, :profile_image, :fill, 50, 50, fallback: "5.jpg", class: "rounded-circle" %>
            </th>
            <th>
              <table class="table table-borderless mt-5">
                <tbody>
                  <tr>
                    <th><%= @user.name %></th>
                  </tr>
                  <tr>
                    <td><%= @user.introduction %></td>
                  </tr>
                </tbody>
              </table>
            </th>
          </tr>
        </tbody>
      </table>
      <div class="col-sm-4 mx-auto">
        <%= link_to " ", edit_user_path(@user), class: "btn btn-info btn-block fas fa-user-cog" %>
      </div>
    </div>
    <div class="col-sm-8 mx-auto mt-5 ml-5 mb-5">
      <% @tasks.each do |task| %>
      <div class=" border border-secondary shadow rounded mt-4">
        <div class="col-sm-12 bg-dark"><i class="fas fa-bell text-dark"></i></div>
        <div class="col-sm-5 mt-3">
          <i class="fas fa-clipboard-list"></i>
          <strong><%= task.title %></strong>（<%= task.user.name %>）
        </div>
        <div class="col-sm-8 mt-3">
          <i class="fas fa-digital-tachograph"></i> <%= task.body %>
        </div>

        <div class="col-sm-3 mt-3 text-success">
          <%= task.status %>
          <i class="fas fa-wrench"></i>
        </div>
        <div class="d-flex flex-row-reverse">
          <%= link_to "", task_path(task), class: "fas fa-clipboard-check btn btn-sm text-info mb-2 ml-3" %>
          <% if current_user %>
            <%= link_to "", edit_task_path(task), class: "fas fa-edit btn btn-sm text-info mb-2 ml-3" %>
          <% end %>
          <p class="mb-2 ml-2"></p>コメント <%= task.comments.count %></p>
          <p class="mb-2 ml-2 mr-3"> ♡ <%= task.likes.count %></p>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
